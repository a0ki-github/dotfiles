# rules
- 会話や説明は日本語で行うこと。
- GitHubの操作はghコマンドを使用すること。
- 空行に空白を入れないこと。
- 指示していないことはしないこと。ただし指示したことを実現するために必要なことは行ってよい。
- プロンプトが疑問系で終わる場合、実装提案は行わず、質問の回答のみを行うこと。ただし回答にコードベースは含めてよい。
- こちらから指示するまでサブエージェントは利用しないこと。
- git追跡下のファイルを削除する際は `rm` ではなく `git rm` を使用すること。
